@extends('layouts.app')

@section('title', 'Input Nilai')

@section('content')
<div class="max-w-7xl mx-auto">
    <!-- Header -->
    <div class="mb-8 text-center">
        <h1 class="text-4xl font-bold text-gray-800 mb-3">Input Nilai Siswa</h1>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">Pilih kelas untuk memulai proses input nilai. Pastikan semua komponen penilaian sudah terkonfigurasi dengan benar.</p>
    </div>

    @if(!isset($kelas) || $kelas->isEmpty())
        <!-- Empty State -->
        <div class="bg-gradient-to-br from-amber-50 to-orange-50 border border-amber-200 rounded-2xl p-8 text-center max-w-2xl mx-auto">
            <div class="w-20 h-20 bg-amber-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-10 h-10 text-amber-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"/>
                </svg>
            </div>
            <h3 class="text-2xl font-bold text-amber-800 mb-3">Data Kelas Belum Tersedia</h3>
            <p class="text-amber-700 mb-2">Anda perlu menambahkan kelas terlebih dahulu sebelum dapat menginput nilai.</p>
            <p class="text-amber-600 text-sm mb-6">Setelah kelas dibuat, Anda dapat menambahkan siswa dan mengkonfigurasi komponen penilaian.</p>
            <a href="{{ route('kelas.index') }}" 
               class="inline-flex items-center gap-3 bg-amber-500 hover:bg-amber-600 text-white font-semibold px-6 py-3 rounded-xl transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-xl">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
                </svg>
                Kelola Data Kelas
            </a>
        </div>
    @else
        <!-- Grid Kelas -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 mb-8">
            @foreach($kelas as $k)
                @if($k && isset($k->id) && isset($k->nama_kelas))
                    <div class="group relative">
                        {{-- PERBAIKAN DI SINI: ganti route('nilai.show') menjadi route('nilai.show-mapel') --}}
                        <a href="{{ route('nilai.show-mapel', ['mapel' => $mapel->id, 'kelas' => $k->id]) }}" 
                           class="block bg-gradient-to-br from-white to-gray-50 rounded-2xl shadow-md hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border border-gray-200 hover:border-blue-300 overflow-hidden">
                            <!-- Header dengan Gradient -->
                            <div class="bg-gradient-to-r from-blue-500 to-indigo-600 p-5 text-white relative overflow-hidden">
                                <div class="absolute top-0 right-0 w-20 h-20 bg-white bg-opacity-10 rounded-full -mr-6 -mt-6"></div>
                                <div class="relative z-10">
                                    <div class="flex items-center justify-between mb-3">
                                        <h3 class="text-xl font-bold truncate">{{ $k->nama_kelas }}</h3>
                                        <div class="bg-white bg-opacity-20 p-2 rounded-lg transform group-hover:scale-110 transition-transform">
                                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/>
                                            </svg>
                                        </div>
                                    </div>
                                    <div class="flex items-center gap-2 text-blue-100 text-sm">
                                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                                        </svg>
                                        <span>Kelas {{ $k->tingkat ?? '-' }}</span>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Content -->
                            <div class="p-5">
                                <!-- Stats -->
                                <div class="grid grid-cols-2 gap-3 mb-4">
                                    <div class="text-center bg-blue-50 rounded-lg p-2">
                                        <div class="text-lg font-bold text-blue-600">{{ $k->siswa_count ?? 0 }}</div>
                                        <div class="text-xs text-blue-500 font-medium">Siswa</div>
                                    </div>
                                    <div class="text-center bg-green-50 rounded-lg p-2">
                                        <div class="text-lg font-bold text-green-600">{{ $k->mapel_count ?? 0 }}</div>
                                        <div class="text-xs text-green-500 font-medium">Mapel</div>
                                    </div>
                                </div>
                                
                                <!-- Status -->
                                <div class="flex items-center justify-between mb-3">
                                    <span class="text-xs font-medium text-gray-500">Status:</span>
                                    <span class="inline-flex items-center gap-1 px-2 py-1 bg-green-100 text-green-700 rounded-full text-xs font-semibold">
                                        <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                                        </svg>
                                        Siap
                                    </span>
                                </div>
                                
                                <!-- CTA -->
                                <div class="flex items-center justify-between pt-3 border-t border-gray-100">
                                    <span class="text-sm text-gray-500 group-hover:text-blue-600 transition-colors font-medium">
                                        Input Nilai
                                    </span>
                                    <div class="transform group-hover:translate-x-1 transition-transform">
                                        <svg class="w-4 h-4 text-gray-400 group-hover:text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                        </svg>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Hover Effect -->
                            <div class="absolute inset-0 border-2 border-transparent group-hover:border-blue-400 rounded-2xl transition-all duration-300 pointer-events-none"></div>
                        </a>
                    </div>
                @endif
            @endforeach
        </div>

        <!-- Quick Stats -->
        <div class="bg-white rounded-2xl shadow-lg border border-gray-200 p-6 mb-8">
            <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                    <div class="bg-blue-100 p-3 rounded-xl">
                        <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                    </div>
                    <div>
                        <h3 class="font-bold text-gray-800">Ringkasan Kelas Tersedia</h3>
                        <p class="text-sm text-gray-600">Total {{ $kelas->count() }} kelas aktif untuk input nilai</p>
                    </div>
                </div>
                <div class="text-right">
                    <div class="text-2xl font-bold text-blue-600">{{ $kelas->count() }}</div>
                    <div class="text-sm text-gray-500">Kelas</div>
                </div>
            </div>
        </div>

        <!-- Tips Section -->
        <div class="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-2xl p-6 border border-blue-200">
            <div class="flex items-start gap-4">
                <div class="bg-blue-100 p-3 rounded-xl flex-shrink-0">
                    <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                </div>
                <div class="flex-1">
                    <h3 class="font-bold text-blue-900 mb-3 text-lg">Panduan Input Nilai</h3>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
                        <div class="bg-white rounded-xl p-4 border border-blue-100">
                            <div class="flex items-center gap-3 mb-2">
                                <div class="w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-sm font-bold">1</div>
                                <span class="font-semibold text-blue-800">Pilih Kelas</span>
                            </div>
                            <p class="text-gray-600 text-xs leading-relaxed">Klik pada kartu kelas yang sesuai untuk melanjutkan proses input nilai</p>
                        </div>
                        <div class="bg-white rounded-xl p-4 border border-green-100">
                            <div class="flex items-center gap-3 mb-2">
                                <div class="w-8 h-8 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold">2</div>
                                <span class="font-semibold text-green-800">Pilih Mapel</span>
                            </div>
                            <p class="text-gray-600 text-xs leading-relaxed">Pilih mata pelajaran dan komponen penilaian yang sudah dikonfigurasi</p>
                        </div>
                        <div class="bg-white rounded-xl p-4 border border-purple-100">
                            <div class="flex items-center gap-3 mb-2">
                                <div class="w-8 h-8 bg-purple-100 text-purple-600 rounded-full flex items-center justify-center text-sm font-bold">3</div>
                                <span class="font-semibold text-purple-800">Input Data</span>
                            </div>
                            <p class="text-gray-600 text-xs leading-relaxed">Masukkan nilai siswa dan sistem akan menghitung nilai akhir secara otomatis</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    @endif
</div>

<!-- Custom Styles -->
<style>
    .group:hover .group-hover\:translate-x-1 {
        transform: translateX(0.25rem);
    }
    
    .shadow-custom {
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }
</style>
@endsection